export interface Materium {
  id?: number;
  nombreMateria: string;
  actividads: string[];
}

//Scaffold-DbContext "server=VICTOR;database=AdminTareas;Integrated Security=True;" Microsoft.EntityFrameworkCore.SqlServer -OutputDir Models -Force
